#include "stdio.h"

int main()
{
    int testCase, input;
    scanf("%d", &testCase);
    
    for (int i=0;i<testCase;i++)
    {
        scanf("%d", &input);
        printf("%s", "output");
    }
    return 0;
}
